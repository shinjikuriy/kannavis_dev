<header>
  <div class="container">
    <h1><%= link_to "kannavis", root_path %></h1>
    <% if user_signed_in? %>
      <%= link_to "sign out", destroy_user_session_path, data: { "turbo-method": :delete } %>
    <% else %>
      <%= link_to "sign in", new_user_session_path %>
    <% end %>
  </div>
</header>